import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const primary = Color(0xFFC62828);
  static const primaryDark = Color(0xFF8E0000);
  static const primaryLight = Color(0xFFD32F2F);
  static const primarySoft = Color(0xFFFFCDD2);
  static const selectionOnRed = Color(0xFFFFEBEE);

  static const white = Color(0xFFFFFFFF);
  static const background = Color(0xFFFAFAFA);
  static const surface = Color(0xFFFFFFFF);

  static const textPrimary = Color(0xFF111827);
  static const textSecondary = Color(0xFF6B7280);
  static const textOnPrimary = Color(0xFFFFFFFF);

  static const border = Color(0xFFE5E7EB);
  static const borderFocused = primary;
  static const error = Color(0xFFDC2626);
  static const warning = Color(0xFFF59E0B);
  static const chartBalance = Color(0xFF2563EB);
  static const disabled = Color(0xFFD1D5DB);
  static const ctaMuted = Color(0xFFE5E7EB);

  static const shadow = Color(0x0F000000);
  static const shadowStrong = Color(0x1A000000);

  static const borderSubtle = Color(0x4D000000);

  // legacy aliases
  static const waveDark = primaryDark;
  static const waveMid = primary;
}
